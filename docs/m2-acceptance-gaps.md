# M2 acceptance evidence and remaining work

This is an evidence index, not a milestone approval. M2 remains in progress; the project initiator retains final review. Reference: the project implementation plan, M2-01 through M2-06 and its unified fault matrix.

| Gate | Direct evidence available | Still required |
| --- | --- | --- |
| M2-01 original transaction and identity | SDK MySQL/GORM commit/rollback/conflict; actual IAM UoW; Mongo active session/reentry/resolved unknown commit; copied immutable bytes | Final provisional contract review together with historical adapter evidence |
| M2-02 bounded Relay and fencing | Bounded batches/concurrency; stop/drain unit tests; real DB conditional writes; database-relative retry; late-result two-Relay test | Operational database clock assumptions and host shutdown contract carried into service integration; do not claim arbitrary hung callbacks can be terminated |
| M2-03 NSQ publishing | Real accepted PUB with lost response becomes unknown; original bytes/identity replay; explicit adapter drain | Final old-wire/catalog mapping validation with service adapters; confirmed still means broker acceptance |
| M2-04 original service schemas | IAM original UoW and historical row comparison; actual historical-table additive-field/fenced-write SQL prototype and unsafe old-writer overlap proof (IAM 8aae3df4); QS original Mongo Runner and historical raw envelope | Review the demonstrated SQL feasibility and mandatory exclusive handoff; complete deployable adapters, migration/indexing and rollback rehearsals remain M3/M4 work, not implied by this prototype |
| M2-05 original consumers | IAM actual FIN loss/redelivery/runtime reconciliation; QS real durable hold and insert failure; host Assessment CAS and Journey reentry; runtime-checkpoint authorization; real FIN-loss redelivery through original Worker handler, loopback gRPC and MySQL intake (QS 996683a2e) | Review consumer-boundary coverage and explicit fixtures: AnswerSheet loading/model validation and direct go-nsq settlement. Full producer intake and production dispatcher composition remain service integration work; frozen/unknown qs-ai semantics stay under M7 ownership |
| M2-06 required faults | MySQL/Relay/NSQ SIGKILL before send and before writeback; real write rejection; confirmation loss; old-claim mutation rejection; late Relay observation; real graceful NSQ restart with preserved test volume and original application bytes | SIGKILL characterization now observes confirmed memory-message loss with the original volume intact. Review the fault-model limit and per-flow compensation requirements before production acceptance; no crash-safe delivery claim follows from a passing characterization. |

The [candidate API/behavior review](m2-review.md) and [scenario-by-scenario fault audit](m2-fault-matrix.md) are available. Next, record current CI and obtain final review of the candidate contracts, host behavior change and observed NSQ crash-loss boundary before merging the drafts. Preserve the shared SDK rather than accumulating service policy in it. M3/M4 production adapters, rollout, performance thresholds and rollback are separate planned gates, not implicitly accepted here.

Later-stage rows in the unified matrix remain tracked: Redis ready-index loss in QS production integration; production cutover/rollback and retention in M3–M6; qs-ai accepted-task discovery, unknown calls, frozen response/config reuse and cancellation capacity in M7. This allocation follows their declared stages and does not remove any requirement. No service replacement or production acceptance has been claimed.
